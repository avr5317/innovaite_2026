# Frontend – copy to .env and change if needed.
# Only VITE_* variables are exposed to the browser (no secrets here).

# API base URL (default: http://localhost:8000/v1)
VITE_API_URL=http://localhost:8000/v1
